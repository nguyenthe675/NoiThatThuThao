@model SHY.Web.Models.PostViewModel

@{
    ViewBag.Title = Model.Name;
    ViewBag.MetaKeyword = Model.MetaKeyword;
    ViewBag.MetaDescription = Model.MetaDescription;
    ViewBag.CUrl = "";
    Layout = "~/Views/Shared/Layouts.cshtml";
}
<link href="~/Assets/client/css/etalage.css" rel="stylesheet" />
<div id="fb-root"></div>
@section footerJS{

}

<div class="single_top">
    <div style="    font-style: italic;">
        <a href="/"><i class="fa fa-home"></i> Trang chủ</a> >> <a href="/tin-tuc.html">Tin tức</a>
    </div>
    <div class="single_grid">

        <h3 style="    font-weight: bold;">@Model.Name</h3>
        <p style="font-size:12px"><i>Ngày đăng: @Model.CreatedDate</i></p>
        <p>@Model.Description</p>

        <div class="clearfix"></div>
    </div>
    <div class="toogle">
        <p class="m_text">@Html.Raw(Model.Content)</p>
    </div>
</div>